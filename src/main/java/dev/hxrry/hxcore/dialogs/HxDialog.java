package dev.hxrry.hxcore.dialogs;

public class HxDialog {
    
    // HxDialog.create("Delete Prefix")
    //     .body("Would you like to delete your prefix?")
    //     .button("Yes", player -> removePrefix(player))
    //     .button("No", player -> {})
    //     .show(player);

    // HxDialog.create("Set Nickname")
    //     .body("Choose how your name appears in chat.")
    //     .field("nickname", "Enter a nickname")
    //     .button("Save", (player, bag) -> setNickname(player, bag.get("nickname")))
    //     .show(player);

    // full builder menu idea would be
    // create(title)
    // .body(text)
    // .field(key, label)
    // .bool(key, label)
    // .range(key, label, min, max)
    // .button(label, player -> ...)
    // .button(label, (player, bag) -> ...)
    // .show(player)

}
